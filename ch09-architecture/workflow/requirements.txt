# Install shared deps first:
#     pip install -r ../../utils/requirements.txt
# (provides pocketflow, pyyaml, markdown-it-py, and your LLM provider SDK.)
#
# This chapter reads config/code off disk and shells out to `git grep`, so a
# working `git` on your PATH is the only extra requirement. No new Python deps.
