# Install shared deps first:
#     pip install -r ../../utils/requirements.txt
# (provides pocketflow, pyyaml, markdown-it-py, and your LLM provider SDK.)
#
# This chapter reads the schema file directly off disk — no extra Python deps.
